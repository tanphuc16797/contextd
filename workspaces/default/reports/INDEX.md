# Reports — workspace `default`

Index of generated reports. Append-only — `/contextd-report` updates row mỗi lần chạy.

## Reports

| Generated | Type | File | Source files |
|-----------|------|------|--------------|
| _(empty)_ |  |  |  |

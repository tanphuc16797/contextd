# Archived Templates

Templates here had no references from `agents/`, `packs/`, `.claude/commands/`, `scripts/`, or `workspaces/` as of the audit. Kept here (not deleted) so they can be revived if a future pack wires them up.

| Template | Likely origin |
|----------|--------------|
| `customer-journey.md` | `pack-product` — never wired into a command |
| `persona.md` | `pack-product` — never wired into a command |
| `product-metrics.md` | `pack-product` — never wired into a command |
| `okr.md` | `pack-product` — never wired into a command |

If a pack needs these, move back to `templates/` and reference from the pack's command/prompt-overrides.
